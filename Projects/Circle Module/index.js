const f = require('./circle');

f.circleArea();
f.circleCircumference();

//Now you can call functions from circle.js here. 
//After reaching the directory of the project in the console:
//Node.js terminal code: node index
